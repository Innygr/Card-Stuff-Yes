# Card Stuff Yes - Server

The backend server for the Card Stuff Yes card game.

## Requirements

- Ubuntu Server
- Node.js
- npm

## Configuration

Server settings are stored in `config.json`.

Current server port:

`6565`

## Running the server

Install dependencies:

```bash
npm install
